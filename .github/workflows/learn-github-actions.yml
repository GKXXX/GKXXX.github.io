name: learn-github-actions
on: [push]
jobs:
check-bats-version:
runs-on: ubuntu-latest
steps:
-uses: actions/vheckout@v2
-uses:action/setup-node@v2
with:
node-version: '14'
-run: npm install -g bats
-run: bats -v
